TURDGOBBLER'S DECK NAME COLOURIFIER — VERSION 4

Open index.html in a modern browser. No install, internet connection, or account is required.

Version 4 is the compact expert build:
- live Arena-ready deck-name gradients
- an always-visible two-to-seven-stop custom gradient editor
- rotate and flip controls for colour order
- repeat-click presets that cycle through every forward colour rotation
- live preset labels that show the active colour order
- editable Rainbow, Sunset, Sunrise, and Silver palettes
- all ten two-colour guilds
- all ten three-colour identities
- W/U/B/R/G controls that filter MTG shortcuts without controlling the gradient
- presets that load editable colours without locking them
- always-visible compact global styling controls
- automatic gradient-stage reduction around Arena's 64-character raw limit
- an exact over-limit count when a result exceeds 64 raw characters
- Ctrl+Enter / Command+Enter copying

Everything runs locally in the browser. Nothing is uploaded.
