VERSION 7 SPRITE ASSET PROVENANCE

STATUS
Included as small functional previews in the free, unofficial Ultimate Version fan utility after the 2026-07-29 provenance and fan-content review. Remove them if a rights holder requests it.

LOCAL SOURCE
- User-installed current MTG Arena Steam build.
- MTGA_Data/resources.assets
- Texture2D path ID 158: EmojiOne, 512 x 512.
- MonoBehaviour path ID 2239: EmojiOne TMP sprite asset.

MAPPING
- TMP metadata describes 16 sprite entries indexed 0 through 15.
- The atlas is a 4 x 4 grid and these 128 x 128 PNG files follow its row-major order.
- Filenames are the exact value emitted by <sprite=n>.
- The small numeric badges are rendered separately by the V7 interface and are not baked into these PNGs.

LIMITATION
The image content and index mapping are authoritative to the locally inspected Arena build. Browser layout, baseline, scale, and spacing remain approximations of Arena rendering.
